package main

import "fmt"

func main() {
	// Only true will run every time in this file
	// If true changed with an expression, then code will run only if returns true, example: "1 < 2"
	if true {
		fmt.Println("It's true")
	}
	if false {
		fmt.Println("It's flase")
	}
}
